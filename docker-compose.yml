services:
  openfit:
    build: .
    container_name: openfit
    restart: unless-stopped
    env_file: .env
    environment:
      - DB_PATH=/data/app.db
      - FIT_DIR=/data/fits
    ports:
      # Internal HTTP only — HTTPS is terminated by a reverse proxy in front.
      # Host port configurable via .env (default 8080); container always listens on 8080.
      - "127.0.0.1:${HOST_PORT:-8080}:8080"
    volumes:
      - openfit_data:/data
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8080/healthz', timeout=5)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

volumes:
  openfit_data:
